# Magnetics & EMI Filter Design
**Document ID:** BLDC-EMI-001

## 1. Noise Sources Identified
[Where does DM noise come from in this design? Where does CM noise come from — e.g., switching node dv/dt coupling to heatsink/chassis via parasitic capacitance?]

## 2. Filter Topology
- **DM filtering:** [series inductor + X-cap — component values and sizing rationale]
- **CM filtering:** [common-mode choke + Y-caps — component values, core material/size chosen and why]

## 3. Component Selection
| Component | Value | Rationale |
|---|---|---|
| DM inductor | | |
| CM choke | | |
| X-cap | | |
| Y-cap | | |

## 4. Core Sizing / Saturation Check
[Show the calculation: expected max DM current, verify it doesn't saturate the CM choke core given the flux-cancellation design.]

## 5. Verification Plan
[How will you confirm the filter works — bench measurement with a current probe/spectrum analyzer, or a simpler proxy test if you don't have EMI test equipment access?]
